import { SlashCommandBuilder } from "discord.js";
import Command from "../classes/Command";

export default new Command(new SlashCommandBuilder().setName("trunc").setDescription("fhdushlkjw"), (slash) => {

    

})